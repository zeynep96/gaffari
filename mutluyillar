<!DOCTYPE html>
<html lang="tr">

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Doğum Günü Kartı - Gaffari Amca (Bilardo Temalı)</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet" />
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            background-color: #3c1d08;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            width: 640px;
            height: 400px;
            position: relative;
            /* eklenmeli! */
            perspective: 1200px;
            transition: 1s;
            display: flex;
            /* kartı yatayda ortala */
            align-items: center;
            justify-content: center;
        }

        .card:hover {
            transform: rotate(-5deg);
        }

        .card:hover .outside {
            transform: rotateY(-130deg);
        }

        .outside,
        .inside {
            height: 100%;
            width: 50%;
            position: absolute;
            left: 50%;
        }

        .inside {
            background: linear-gradient(to right, #4a2a1b, #f0f0f0 1%);
            /* İç kartın sol tarafı kahve, sağa doğru açılan renk */
            color: #333;
            /* İç yazı rengi */
            line-height: 1.5;
            padding: 0 20px;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: space-around;
            align-items: center;
        }

        .outside {
            -webkit-transform-style: preserve-3d;
            transform-style: preserve-3d;
            z-index: 1;
            transform-origin: left;
            transition: 2s;
            cursor: pointer;
        }

        .front,
        .back {
            height: 100%;
            width: 100%;
            position: absolute;
            -webkit-backface-visibility: hidden;
            backface-visibility: hidden;
            transform: rotateX(0deg);
        }

        .front {
            background-color: #104728;
            /* Kartın ön yüzü yeşil - Bilardo masası örtüsü */
        }

        .back {
            transform: rotateY(180deg);
            background: linear-gradient(to left, #2e170d, #f0f0f0 1%);
            /* Arka yüzü de temaya uygun */
        }

        .cake {
            width: 100%;
            position: absolute;
            bottom: 30px;
        }

        .top-layer,
        .middle-layer,
        .bottom-layer {
            height: 80px;
            width: 240px;
            background-repeat: repeat;
            background-size: 60px 100px;
            background-position: 28px 0;
            /* Pasta katmanları renkleri - Koyu kahve ve krem tonları */
            background-image: linear-gradient(transparent 50px,
                    #8b4513 50px,
                    /* Pasta içi koyu kahve */
                    #ffe4b5 60px,
                    /* Krem rengi */
                    transparent 60px),
                radial-gradient(circle at 30px 5px, #4d260c 30px, #e6c278 31px);
            /* Üst krem ve süsleme */
            border-radius: 10px 10px 0 0;
            position: relative;
            margin: auto;
        }

        .middle-layer {
            transform: scale(0.85);
            top: 6px;
        }

        .top-layer {
            transform: scale(0.7);
            top: 26px;
        }

        .candle {
            height: 45px;
            width: 15px;
            background: repeating-linear-gradient(45deg,
                    #dc143c 0,
                    /* Koyu kırmızı mum */
                    #dc143c 5px,
                    #ff6347 5px,
                    /* Açık kırmızı şerit */
                    #ff6347 10px);
            position: absolute;
            margin: auto;
            left: 0;
            right: 0;
            bottom: 202px;
        }

        .candle:before {
            content: "";
            position: absolute;
            height: 16px;
            width: 16px;
            background-color: #ffd700;
            /* Altın rengi alev */
            border-radius: 0 50% 50% 50%;
            bottom: 48px;
            transform: rotate(45deg);
            left: -1px;
        }

        .outside p {
            font-size: 20px;
            text-transform: uppercase;
            margin-top: 30px;
            text-align: center;
            letter-spacing: 4px;
            color: #f0f0f0;
            /* Yeşil zemin üzerinde daha açık okunur yazı */
            padding: 0 10px;
        }

        .inside h1 {
            font-size: 120px;
            line-height: 120px;
            color: #3c1d08;
            /* İçteki hediye emojisi için koyu renk */
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="outside">
            <div class="front">
                <p>Nice Mutlu Yaşlara Gaffari Amca</p>
                <div class="cake">
                    <div class="top-layer"></div>
                    <div class="middle-layer"></div>
                    <div class="bottom-layer"></div>
                    <div class="candle"></div>
                </div>
            </div>
            <div class="back"></div>
        </div>
        <div class="inside">
            <p>Gönlünüzden geçen tüm güzelliklerin gerçekleştiği, mutlu ve sağlıklı nice mutlu yaşlar dilerim.
            </p>
            <h1>&#127873;</h1>
        </div>
    </div>
</body>

</html>
